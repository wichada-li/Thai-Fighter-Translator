{
  "name": "thai-fighter-translator",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "apt-get install -y python3 || true"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
